{
  "name": "contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "truffle test",
    "compile": "truffle compile",
    "deploy:mumbai": "truffle migrate --compile-all --network mumbai --f 1 --to 1",
    "deploy:mumbai:verify": "truffle run verify GamerTag --network mumbai"
  },
  "devDependencies": {
    "@openzeppelin/test-helpers": "^0.5.15",
    "@truffle/hdwallet-provider": "^2.0.1",
    "truffle-plugin-verify": "^0.5.21"
  }
}
